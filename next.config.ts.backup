import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This clinic site currently has no backend/payment requirement, so static
  // export keeps hosting simple and inexpensive. Remove `output: "export"`
  // later if server features are introduced.
  output: "export",
  trailingSlash: true,
  images: { unoptimized: true },
};

export default nextConfig;
